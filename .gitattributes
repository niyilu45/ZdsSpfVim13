# Keep installer/configuration bytes stable after cloning on Windows,
# regardless of the user's core.autocrlf setting.
* -text
config/* text eol=lf
*.ps1 text eol=lf
*.bat text eol=crlf
*.zip binary
